# Reference scripts

Drop example reel/short scripts in this folder (as `.txt` or `.md` files) — scripts you like the style of, or your own past scripts.

When this skill is invoked, it reads everything here first and matches its tone, pacing, and phrasing instead of relying on the generic templates in `SKILL.md`. Empty for now.
